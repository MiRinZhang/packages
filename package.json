{
  "name": "@mirinzhang/root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "cleanBuild": "rm -rf packages/*/dist",
    "build": "node scripts/build.js",
    "publish": "lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^3.9.6"
  }
}
